#include <stdio.h>
#include <stdlib.h>
#include "MyArray.h"

int main(){
    // CrossList M;
    // M.rhead = M.chead = NULL;
    // M.mu = M.nu = M.tu = 0;
    // CreatSMatrix_OL(&M);
    // PrintSMatrix_OL(M);
    printf("%d", INT_MAX);
    return 0;
}

